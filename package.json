{
  "name": "cleanweb",
  "version": "0.2.0",
  "description": "Search without the noise — custom search frontend with ruthless filtering and family collections.",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "vercel dev"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@mozilla/readability": "^0.6.0",
    "@upstash/redis": "^1.34.3",
    "linkedom": "^0.18.12",
    "marked": "^18.0.2"
  }
}
